dist: trusty
language: python
python:
 - "3.5"
install:
 - pip install -r requirements.txt
 - pip install coveralls
script:
  sh run_travis.sh
  coverage run test_prepare_data.py
after_success:
  coveralls